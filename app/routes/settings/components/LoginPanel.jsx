import React from "react"
import ItemPanel from "../../shared/components/ItemPanel"

const LoginPanel = () => {
  return (
    <ItemPanel
      title="Account"
      subtitle="You are not logged in. Enter an assignment URL to sign in."
    />
  )
}

export default LoginPanel
