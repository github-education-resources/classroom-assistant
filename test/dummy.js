/* eslint-env mocha */

const assert = require("chai").assert

describe("arithmetic", () => {
  describe("numbers", () => {
    it("should add up correctly", () => {
      assert.equal(2, 1 + 1)
    })
  })
})
